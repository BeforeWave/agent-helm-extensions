{
  "name": "agent-helm-installer",
  "version": "0.1.3",
  "private": true,
  "type": "module",
  "scripts": {
    "build:pkg": "node build.mjs",
    "test": "node --test tests/*.test.mjs",
    "verify": "node --check build.mjs && node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
